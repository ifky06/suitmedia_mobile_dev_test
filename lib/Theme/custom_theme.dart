import 'package:flutter/material.dart';

class CustomTheme {
  static const screenPadding = EdgeInsets.all(24);

  static const darkBlue = Color(0xff2b637b);
  static const lightBlue = Color(0xff554AF0);
}
